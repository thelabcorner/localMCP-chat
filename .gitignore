node_modules/
.idea/
out/
release/
release-next/
# Fresh local package-validation outputs used when the default release directory is locked.
release-*-validate/
# Fresh unpacked rollout builds used for local install/restart validation.
release-*-rollout/

# Generated by scripts/make-icon.mjs during the build.
build/

# Downloaded by scripts/fetch-tunnel-client.mjs during the build.
resources/

*.log
# Local environment and credential files. Never publish real secrets.
.env
.env.*
!.env.example
*.pem
*.p12
*.pfx
.DS_Store
Thumbs.db
*.har
.public-audit/

# Internal handoffs and machine-specific research notes are never publication inputs.
docs/handoff/
docs/internal/

# Internal working notes and forensic audits. Kept locally, not published:
# they are a detailed map of unfixed weaknesses in a tool that runs commands
# on the user's machine. Architecture lives in AGENTS.md instead.
TODO.md
CLAUDE_LIVE_FINDINGS.md
TOOL_CALL_ISSUES.md
TOOL_USABILITY_AUDIT.md
bughunt-*/
docs/evidence/
docs/android-mcp-brainstorm/

# Smoke-test scratch fixtures.
smoke/
smoke-switch.txt
HANDOFF.md
docs/handoff-*.md
docs/open-issues-*.md

# One-off local experiment scratch.
one-cycle.ps1
.one-cycle-*
proof-*.txt
